```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Navigates to /spa
    Browser->>Server: GET /spa
    Server-->>Browser: Returns base HTML (with <script> tag)

    Browser->>Server: GET /main.js
    Server-->>Browser: Sends JavaScript for SPA

    Browser->>Browser: Executes JS and initializes SPA logic
    Browser->>Server: GET /data.json
    Server-->>Browser: Returns JSON with notes

    Browser->>Browser: Renders notes dynamically in the DOM
    Browser->>User: Displays interactive UI (without page reload)