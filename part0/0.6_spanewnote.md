```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Writes note content and clicks "Save"
    
    Note right of Browser: JS captures the form submission event
    Browser->>Browser: Prevent default page reload
    Browser->>Browser: Create a note object with content and timestamp

    Browser->>Server: POST /new_note_spa with JSON
    Server-->>Browser: Returns status OK (200)

    Browser->>Browser: Add the new note to the notes list in memory
    Browser->>Browser: Re-render the notes list in the DOM
    Browser->>User: UI updates immediately without reload
