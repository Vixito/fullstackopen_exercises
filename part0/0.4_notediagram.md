```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Type text in the field
    User->>Browser: Click on the "Save" button
    
    Note right of Browser: JS intercepts the submit event
    Browser->>Browser: Prevent default behavior
    Browser->>Browser: Create JSON object with note

    Browser->>Server: POST /new_note con JSON
    Server->>Server: Extract and save the note
    Server-->>Browser: HTTP 302 Redirect to /notes

    Browser->>Server: GET /notes
    Server-->>Browser: HTML with updated note list
    Browser->>User: HTML with updated note list