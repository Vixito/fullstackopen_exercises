```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Type text in the field
    User->>Brower: Click on the "Save" button
    
    Note right of Brower: JS intercepts the submit event
    Brower->>Brower: Prevent default behavior
    Brower->>Brower: Create JSON object with note

    Brower->>Server: POST /new_note con JSON
    Server->>Server: Extract and save the note
    Server-->>Brower: HTTP 302 Redirect to /notes

    Brower->>Server: GET /notes
    Server-->>Brower: HTML with updated note list
    Brower->>User: HTML with updated note list